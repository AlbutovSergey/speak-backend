# Speak Backend

Серверная часть мессенджера Speak.

## Переменные окружения

- `DATABASE_URL` — строка подключения к PostgreSQL (Supabase)
- `SESSION_SECRET` — секретный ключ для сессий

## Запуск

```bash
npm install
npm start
